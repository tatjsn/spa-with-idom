{
  "name": "spa-idom",
  "version": "0.0.0",
  "type": "module",
  "files": [
    "/dist"
  ],
  "scripts": {
    "lint": "prettier --check .",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:npm": "vite build --config npm.config.js"
  },
  "devDependencies": {
    "express": "^4.18.1",
    "prettier": "^2.7.1",
    "vercel": "^28.2.5",
    "vite": "^3.1.0"
  },
  "dependencies": {
    "incremental-dom": "^0.7.0"
  }
}
